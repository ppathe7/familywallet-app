spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/familywallet_dev
    username: familywallet_dev
    password: familywallet_dev
  
  redis:
    host: localhost
    port: 6379
    database: 1

logging:
  level:
    root: INFO
    com.familywallet: DEBUG
